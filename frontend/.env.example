# Backend API URL — leave empty for local development (Vite proxy handles it)
# Set to your deployed backend URL for production (e.g., GitHub Pages):
#   VITE_API_URL=https://your-backend-host.com
VITE_API_URL=
